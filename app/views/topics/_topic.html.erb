<div class="media">
   <div class="media-body">
     <small>
       <%= topic.user.name %> created <%= time_ago_in_words(comment.created_at) %> ago
       <% if user_is_authorized_for_topic?(topic) %>
         | <%= link_to "Delete", [@topic], method: :delete %>
       <% end %>
     </small>
     <p><%= topic.description %></p>
   </div>
 </div>
